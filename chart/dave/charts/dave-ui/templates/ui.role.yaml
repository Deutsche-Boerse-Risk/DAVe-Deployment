kind: Role
apiVersion: rbac.authorization.k8s.io/v1alpha1
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: dave
    layer: frontend
    project: dave
  namespace: {{ .Release.Namespace }}
rules:
